.venv/
__pycache__/
*.pyc
.pytest_cache/
output/*
!output/filed/
!output/.gitkeep
# live tracker with signer data never goes in git; the generated blank template is under output/
Petition Captain Master Tracker*.xlsx
*.local.yaml
.env
.DS_Store
*.egg-info/
dist/
# the sender's signature for the records-request letters (personal; never committed)
config/signature*.png
# the plain response tokens printed in the letters (the committed file holds only their hashes)
config/tokens.local.json
